.deps/

# Generated by :helptags; the .txt sources are committed, the index is not.
doc/tags

# runtime-analysis.env (docs/ROADMAP.md §2.1): the per-machine half of the
# request-runner's environment files — real tokens belong here, never in
# http-client.env.json, which is meant to be committed.
http-client.private.env.json
